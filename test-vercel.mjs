import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch({ args: ['--no-sandbox', '--disable-setuid-sandbox'] }); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', error => console.log('PAGE ERROR:', error.message)); try { await page.goto('https://gina-docente.vercel.app/', { waitUntil: 'networkidle0', timeout: 15000 }); } catch(e){} await new Promise(r => setTimeout(r, 2000)); await browser.close(); process.exit(0); })();
